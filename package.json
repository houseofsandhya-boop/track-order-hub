{
  "name": "track-order-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "shopify app dev",
    "serve": "node web/server.js",
    "start": "node web/server.js",
    "build": "node --check web/server.js",
    "deploy": "shopify app deploy",
    "check": "node --check web/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@preact/signals": "^2.9.4",
    "@shopify/ui-extensions": "latest",
    "preact": "^10.26.4"
  }
}
